<%= form_for(nautical_sport) do |f| %>
  <% if nautical_sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nautical_sport.errors.count, "error") %> prohibited this nautical_sport from being saved:</h2>

      <ul>
      <% nautical_sport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
