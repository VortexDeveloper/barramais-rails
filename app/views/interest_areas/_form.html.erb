<%= form_for(interest_area) do |f| %>
  <% if interest_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(interest_area.errors.count, "error") %> prohibited this interest_area from being saved:</h2>

      <ul>
      <% interest_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, value: @interest_area.name%>
  </div>

  <div class="field">
    <%= f.label :photo %>
    <%= f.file_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
